const express = require('express')
const router = express.router()

router.get('/bills', (req, res) => {
  // fetch from sunlight apiEndpoint
  // turn into json
  // structure Object
  // respond with structured object
})

import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class DumbName extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

DumbName.propTypes = propTypes;
export default DumbName;
